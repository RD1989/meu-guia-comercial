/**
 * SilentSeeder - Garante que o banco de dados tenha dados de demonstração 
 * via migrations SQL no Supabase. Evita concorrência e mutações anônimas no client-side.
 */
export const SilentSeeder = () => {
  return null;
};
